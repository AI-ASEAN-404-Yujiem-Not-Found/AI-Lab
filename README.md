# AI LAB for AI ASEAN
This repository consist of data that will be used to train model and the models development proccess include the model it self. The data and model not will be shown in this repository, instead the model and data will be uploaded via this link `(nanti yah)`

## Worklfow and Collaboration Setup
To syncronize our works please :
- everytime you create data (generate dummy data or find data via api or raw data) call this function **syncStoreMetaData**

```python
```

- everytime you create a model (train model, fine tune, create proxy ai api or anything else) call this function **syncModelMetaData**

```python
```

- see all the data overview like this (call storeMetadata)
```python
```

`ach will handle`